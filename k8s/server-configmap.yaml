apiVersion: v1
kind: ConfigMap
metadata:
  name: server-config
data:
  PORT: "5000"
  NODE_ENV: "production"
  MONGO_URI: "mongodb://mongo-service:27017/todo_app"
  FRONTEND_URL: "http://localhost:3000" # Update this to the actual frontend LoadBalancer/Ingress URL in production
  LOCAL_FRONTEND_URL: "http://localhost:3000"
